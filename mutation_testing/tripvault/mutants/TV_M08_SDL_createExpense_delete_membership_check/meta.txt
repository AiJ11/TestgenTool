Mutation ID    : TV_M08
Operator       : SDL
File           : controllers/ExpensesController.js
Description    : [SDL] createExpense: delete membership check
Result         : EXPECTED_KILLED
Killing tests  : test08_unauthorizedExpense

ORIGINAL:
        if (!isMember) {
            if (req.file) await fs.unlink(req.file.path);
            return res.status(403).json({ message: "You are not a member of this trip" });
        }

MUTANT:
        // [MUTANT TV_M08: SDL] membership check deleted
