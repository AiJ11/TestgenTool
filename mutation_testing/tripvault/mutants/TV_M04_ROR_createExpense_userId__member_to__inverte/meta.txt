Mutation ID    : TV_M04
Operator       : ROR
File           : controllers/ExpensesController.js
Description    : [ROR] createExpense: userId === member → !== (inverted membership check)
Result         : EXPECTED_KILLED
Killing tests  : test05_createExpense, test08_unauthorizedExpense

ORIGINAL:
        const isMember = trip.members.some(member => member.userId === userId) || trip.createdBy === userId;

MUTANT:
        const isMember = trip.members.some(member => member.userId !== userId) || trip.createdBy === userId;
