Mutation ID    : EC_M02
Operator       : SDL
File           : controllers/authController.js
Description    : [SDL] register: delete duplicate-user check
Result         : EXPECTED_KILLED
Killing tests  : test22_loginWithoutRegister, test24_duplicateRegistration

ORIGINAL:
    const existingUser = await User.findOne({ email });
    if (existingUser) {
      return res.status(400).json({ error: 'User already exists' });
    }

MUTANT:
    // [MUTANT EC_M02: SDL] duplicate-user check deleted
