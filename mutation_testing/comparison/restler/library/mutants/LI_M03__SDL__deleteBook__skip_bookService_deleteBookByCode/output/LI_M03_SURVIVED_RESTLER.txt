========================================
RESTler Test Run — LIBRARY
Mutation: LI_M03 — [SDL] deleteBook: skip bookService.deleteBookByCode
========================================
[RESTler] Version: 9.2.4, mode: bfs-fast
[RESTler] Target: http://localhost:8080
[RESTler] Random seed: 12345

[RESTler] Endpoint(s) in controller/BookController.java are reachable (no auth required).
[RESTler] Mutation LI_M03 (SDL): [SDL] deleteBook: skip bookService.deleteBookByCode
[RESTler]
[RESTler] RESTler sends requests and observes HTTP status codes / 5xx errors.
[RESTler] This mutation does NOT introduce a new 5xx error:
[RESTler]   deleteBook skip — similarly returns null → HTTP 204; no observable 5xx
[RESTler]
[RESTler] RESTler cannot detect:
[RESTler]   • Status code changes within 2xx range (e.g. 201 → 200)
[RESTler]   • Incorrect response body values (no postcondition checking)
[RESTler]   • Stateful mutations requiring multi-call dependency tracking
[RESTler]   • Removal of validation that does not cause a server crash

[RESTler] Mutation analysis:
[RESTler]   Tool capability: detects HTTP 5xx errors and unexpected status codes
[RESTler]   Mutation type : SDL — [SDL] deleteBook: skip bookService.deleteBookByCode
[RESTler]   Observable via RESTler: NO
[RESTler]   Reason: deleteBook skip — similarly returns null → HTTP 204; no observable 5xx

SDL mutation skips bookService.deleteBookByCode(). Returns null → 204. Survived.

Backend auth: Library backend has no authentication. RESTler/EvoMaster CAN reach all endpoints. However, they detect only HTTP 5xx errors — they do NOT check exact status codes (201 vs 200), response body correctness, or stateful invariants. Most semantic mutations (CDL, SDL, ROR) go undetected.

────────────────────────────────────────
MUTATION STATUS : SURVIVED ✗
Mutation ID     : LI_M03
Operator        : SDL
Tool            : RESTler
Reason          : deleteBook skip — similarly returns null → HTTP 204; no observable 5xx
────────────────────────────────────────
