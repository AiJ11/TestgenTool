Mutation ID    : GS_M51
Operator       : SDL
File           : controllers/AppController.js
Description    : [SDL] verifyOtp: skip UserDeviceLinks.create on existing device
Result         : EXPECTED_KILLED
Killing tests  : test04_getMyDevices, test05_getDeviceInfo

ORIGINAL:
      await DBUserDeviceLinks.create({
        deviceId,
        userId: otpData.userId,
        role: "owner"
      });
      return res.status(200).json({ message: "OTP verified"});
    } 
    // Create a new device in the database

MUTANT:
      // [MUTANT GS_M51: SDL] UserDeviceLinks.create deleted (existing-device path)
      return res.status(200).json({ message: "OTP verified"});
    } 
    // Create a new device in the database
