Mutation ID    : GS_M03
Operator       : UOI
File           : controllers/SessionController.js
Description    : [UOI] createSession: negate ownership guard
Result         : EXPECTED_KILLED
Killing tests  : test06_createSession, test14_createSessionForbidden

ORIGINAL:
const userDeviceLink = await DBUserDeviceLinks.findOne({ userId, deviceId, role: "owner" });
        if (!userDeviceLink) {

MUTANT:
const userDeviceLink = await DBUserDeviceLinks.findOne({ userId, deviceId, role: "owner" });
        if (userDeviceLink) {
