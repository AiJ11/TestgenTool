Mutation ID    : GS_M16
Operator       : ROR
File           : controllers/SessionController.js
Description    : [ROR] joinSession: expiry check < → >
Result         : EXPECTED_KILLED
Killing tests  : test07_joinSession, test11_getOtherDevices, test17_fullSessionLifecycle

ORIGINAL:
if (session.expiry && new Date(session.expiry) < new Date()) {

MUTANT:
if (session.expiry && new Date(session.expiry) > new Date()) {
